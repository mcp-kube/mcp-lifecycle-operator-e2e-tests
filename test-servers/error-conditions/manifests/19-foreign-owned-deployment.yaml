---
# Test Case: Accepted=False, Reason=Invalid
# Scenario: MCPServer tries to manage a Deployment owned by another controller
# This manifest will be used after pre-creating a foreign-owned Deployment
apiVersion: mcp.x-k8s.io/v1alpha1
kind: MCPServer
metadata:
  name: foreign-owned-deployment
  namespace: default
spec:
  source:
    type: ContainerImage
    containerImage:
      ref: ghcr.io/modelcontextprotocol/servers/everything:latest
  config:
    port: 8080
