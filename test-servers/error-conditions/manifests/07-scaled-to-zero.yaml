---
# Test Case: Ready=True, Reason=ScaledToZero
# Scenario: Deployment with 0 replicas (intentional, valid state)
apiVersion: mcp.x-k8s.io/v1alpha1
kind: MCPServer
metadata:
  name: error-scaled-to-zero
  namespace: default
spec:
  source:
    type: ContainerImage
    containerImage:
      ref: ghcr.io/modelcontextprotocol/servers/everything:latest
  config:
    port: 8080
  runtime:
    replicas: 0
