---
# Test Case: Accepted=False, Reason=Invalid
# Scenario: Missing Secret referenced in envFrom
apiVersion: mcp.x-k8s.io/v1alpha1
kind: MCPServer
metadata:
  name: error-missing-secret-envfrom
  namespace: default
spec:
  source:
    type: ContainerImage
    containerImage:
      ref: ghcr.io/modelcontextprotocol/servers/everything:latest
  config:
    port: 8080
    envFrom:
      - secretRef:
          name: this-secret-does-not-exist
